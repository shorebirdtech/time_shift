name: Shorebird Patch

on:
  push:
  workflow_dispatch:

jobs:
  create_patch:
    defaults:
      run:
        shell: bash

    runs-on: ubuntu-latest

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v3

      - name: 🐦 Setup Shorebird
        uses: shorebirdtech/setup-shorebird@68f5f51c567f60526a7d7259c7acdb407cc9abc4

      - name: ✨ Create Patch
        run: shorebird patch android --force
        env:
          SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_TOKEN }}
